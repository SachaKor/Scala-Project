export const PATHS = {
  HOME: '/',
  LOGIN: '/login',
  SIGNIN: '/signin',
  BOARD: '/board',
  LOBBY: '/lobby'
}

export const PATHS_NAV = [
  PATHS.HOME,
  PATHS.BOARD,
  PATHS.LOBBY
]

export const PATHS_NO_NAV = [PATHS.LOGIN, PATHS.SIGNIN]
